name: Create Release on PR Merge

on:
  pull_request:
    types:
      - closed

jobs:
  create-release:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == true
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      
      - name: Get latest tag
        uses: actions-ecosystem/action-get-latest-tag@v1
        id: get-latest-tag

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Get PR title
        run: |
          PR_TITLE=$(curl -sSL https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }} | jq -r '.title')
          echo "PR_TITLE=$PR_TITLE" >> $GITHUB_ENV

      - name: Get UpdateVers Script
        run: echo "${{ vars.UPDATE_VERS_SCRIPT }}" > updateVers.py
        shell: bash

      - name: Update Version
        run: |
          output=$(python updateVers.py '${{ steps.get-latest-tag.outputs.tag }}' '${{ env.PR_TITLE }}')
          echo "::set-output name=nexttag::$output"
        id: create-next-tag

      - name: Get Commit SHA of Merged PR
        run: |
          PR_MERGE_SHA=$(curl -sSL https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/merge | jq -r '.sha')
          echo "PR_MERGE_SHA=$PR_MERGE_SHA" >> $GITHUB_ENV

        # GITHUB_SHA

      - name: Create Tag
        run: |
          git tag -f -a ${{ steps.create-next-tag.outputs.nexttag }} $PR_MERGE_SHA
          git push -f origin ${{ steps.create-next-tag.outputs.nexttag }}

      - name: Create release
        uses: actions/create-release@v1
        with:
          tag_name: ${{ steps.create-next-tag.outputs.nexttag }}
          release_name: ${{ steps.create-next-tag.outputs.nexttag }}
          body: ${{ env.PR_TITLE }}
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}
